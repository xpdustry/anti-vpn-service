package service.providers.types;

public abstract class JsonAddressProvider extends AddressProvider {
  public JsonAddressProvider(String displayName, String name) { super(displayName, name); }
  public JsonAddressProvider(String name) { super(name); }
  
  
}
